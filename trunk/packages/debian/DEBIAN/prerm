#!/usr/bin/env python

"""
	@author: Jean-Lou Dupont
"""

import os
import sys

cwd     = os.getcwd()
id      = "$Id$"

print "LITM: cwd[%s] id[%s]" % (cwd, id)

command = sys.argv[1]

if command is 'remove':
	os.system("ldconfig")
	

sys.exit(0)