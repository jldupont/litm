#!/usr/bin/env python

"""
	@author: Jean-Lou Dupont
"""

import os
import sys

cwd     = os.getcwd()
id      = "$Id$"

print "LITM: cwd[%s] id[%s]" % (cwd, id)

try:
	command = sys.argv[1]
	print "LITM: postrm: command[%s]" % command
except:
	pass

sys.exit(0)